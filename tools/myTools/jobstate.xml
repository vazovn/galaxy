<tool id="jobstate" name="Job files" version="0.1">
  <description>check the state of your job</description>
  
  <command interpreter="python">
    jobstate.py $jobid $dirlist.extra_files_path > $dirlist
    ####2>jobstategalaxy_error
  </command>

 
  <inputs>
    <param name="jobid" size="8" type="integer" value="" label="Lifeportal job id" help="TheLifeportal id can be found under 'i' icon in the history items."/>
  </inputs>

  <outputs>
    <data format="html" name="dirlist" label="Files for job ${jobid}" /> 
  </outputs>

  <help>

**About the tool**

This simple tool lets you see the content of a job directory.  It can be used to view the state of your job.
  </help>
</tool>

