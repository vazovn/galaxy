<tool id="BEAST" name="BEAST (1.7.5)" version="1.7.5">
  <description>Bayesian MCMC analysis of molecular sequences.</description>
  <parallelism method="basic"></parallelism>

  <command>
    ulimit -s 4000000;
    module load beast/1.7.5;
    beast -threads 3 -seed $seed -beagle $xmlfile>beastout
    2>beasterr;
    tar --exclude='*galaxy*' -cvf all_beast_res.tar *

  </command>

  <requirements>
    <requirement type="binary">beast</requirement>
  </requirements>

  <inputs>
    <param name="xmlfile" type="data" label="Input XML file" help="" optional="false" format="txt" />
    <param name="seed" size="8" type="integer" value="12345" label="Random seed (-seed)"/>
  </inputs>

  <outputs>
    <data format="txt" name="stdout" label="BEAST on ${xmlfile.name} - stdout" from_work_dir="beastout"/>
    <data format="tar" name="results" label="BEAST on ${xmlfile.name} - results" from_work_dir="all_beast_res.tar"/>
  </outputs>

  <help>

**Job parameters**

BEAST uses threading - use 4 for Number of CPUs per node


**About BEAST**

BEAST is a cross-platform program for Bayesian MCMC analysis of molecular sequences. It is entirely orientated towards rooted, time-measured phylogenies inferred using strict or relaxed molecular clock models. It can be used as a method of reconstructing phylogenies but is also a framework for testing evolutionary hypotheses without conditioning on a single tree topology. BEAST uses MCMC to average over tree space, so that each tree is weighted proportional to its posterior probability. We include a simple to use user-interface program for setting up standard analyses and a suit of programs for analysing the results.
 
**Home page**: http://beast.bio.ed.ac.uk/

**Input**: XML file generated from Nexus file using BEAUTi (http://beast.bio.ed.ac.uk/BEAUti)

**Output**: Results "BEAST results" are returned in a form of a tar file.  Use "tar xvf file_name" to extract. On windows, 7-zip (www.7-zip.org) can be used.


  </help>
</tool>
