name: filesender
type: 
  - masthead
activate: true

icon: fa-stack-overflow
tooltip: Filesender Lifeportal

function: >  
    $.getJSON("/api/webhooks/filesender/get_data", 
        function(data) {window.open('https://FILESENDER_URL', '_blank');}
    );
